export type User = {
  _id: number;
  fullName: string;
  email: string;
  phoneNumber: string;
  password: string;
  regNumber: string;
  level: string;
  createdAt: string;
  school: string;
};
export type PageMetaData = { page: number; pages: number; totalItems: 177 };
